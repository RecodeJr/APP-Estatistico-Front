name: app_estatistico_novo
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.0.2
  jaguar_jwt: ^3.0.0
  shared_preferences: ^2.0.15
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  flutter_screenutil: ^5.9.0
  string_validator: ^1.0.0
  flutter_staggered_grid_view: ^0.7.0
  open_file: ^3.3.2
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/telas_referencias/
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg